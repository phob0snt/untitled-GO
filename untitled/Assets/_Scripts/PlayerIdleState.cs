using UnityEngine;

public class PlayerIdleState
{
    //private PlayerStateMachine _playerStateMachine;

    //public PlayerIdleState(PlayerStateMachine stateMachine)
    //{
    //    _playerStateMachine = stateMachine;
    //}

    //public void Enter()
    //{

    //}
    //public void Update()
    //{
    //    if (Mathf.Abs(_playerStateMachine.PlayerController.JoystickInput.x) > 0 || Mathf.Abs(_playerStateMachine.PlayerController.JoystickInput.y) > 0)
    //    {
    //        _playerStateMachine.ChangeState(new PlayerRunState(_playerStateMachine));
    //    }
    //}

    //public void Exit()
    //{

    //}
}
